function townInfo(towninput, populationInput, areaInput) {
    const town = towninput;
    const population = populationInput;
    const area = areaInput;

    console.log(`Town ${town} has population of ${population} and area ${area} square km.`);
}

townInfo('Sofia', 1286383, 492);