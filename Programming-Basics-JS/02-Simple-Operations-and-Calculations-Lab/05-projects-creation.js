function projectsCreation(name, number) {
    let hours = number * 3
    console.log(`The architect ${name} will need ${hours} hours to complete ${number} project/s.`);
}

projectsCreation("Ivan", 9);